package geeks.strings;
//import org.junit.Assert;
//import static org.junit.Assert.*;
//import org.junit.Test;
//import org.json.simple.JSONObject;
public class JunitTest {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		/*
		try (MockedStatic<Inverter> theMock = Mockito.mockStatic(Inverter.class)) {
		    theMock.when(Inverter::invert("a")).thenReturn("a");
		    assertThat(Inverter.invert("a")).isEqualTo("a");
			theMock.when(Inverter::invert(null)).thenReturn("");
		    assertThat(Inverter.invert(null)).isEqualTo("");
			theMock.when(Inverter::invert("abcd")).thenReturn("dcba");
		    assertThat(Inverter.invert("abcd")).isEqualTo("dcba");
		  } */
		
		//JSONObject result = new JSONObject();

	}
	
	
/*
    @Test
    public void shouldInvert() {
        String inverted = Inverter.invert(null);
        assertEquals
        Assert.fail();
    }

    */
  
}

class Inverter {
	
	public static String invert(String s){
		
		return "";
	}
}
